apiVersion: v1
kind: Service
metadata:
  name: notifier
  namespace: app
  labels:
    app: notifier
    team: greenlogistics
    env: production
spec:
  selector:
    app: notifier
  ports:
    - name: http
      port: 3003
      targetPort: 3003
